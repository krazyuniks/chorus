# generated by datamodel-codegen:
#   filename:  quoting_queue_route_args.schema.json

from enum import StrEnum
from typing import Annotated

from pydantic import BaseModel, ConfigDict, Field


class ProductFamilyCategory(StrEnum):
    MOTOR_PRIVATE_CAR = "motor_private_car"
    MOTOR_COMMERCIAL_VEHICLE = "motor_commercial_vehicle"
    HOME_BUILDINGS = "home_buildings"
    HOME_CONTENTS = "home_contents"
    HOME_COMBINED = "home_combined"
    TRAVEL_SINGLE_TRIP = "travel_single_trip"
    TRAVEL_ANNUAL_MULTI_TRIP = "travel_annual_multi_trip"
    PET = "pet"


class QuotingQueueRouteArgs(BaseModel):
    model_config = ConfigDict(
        extra="forbid",
    )
    enquiry_ref: Annotated[str, Field(pattern="^enq_[A-Za-z0-9_-]+$")]
    customer_ref: Annotated[str, Field(pattern="^cust_[A-Za-z0-9_-]+$")]
    product_family_category: ProductFamilyCategory
    qualification_summary_ref: Annotated[
        str,
        Field(
            description="Reference to the qualification summary the verdict was based on.",
            pattern="^qsum_[A-Za-z0-9_-]+$",
        ),
    ]
    verdict_ref: Annotated[
        str,
        Field(
            description="Qualification verdict reference driving the routing. Idempotency anchor.",
            pattern="^verdict_[A-Za-z0-9_-]+$",
        ),
    ]
    routing_policy_ref: Annotated[str, Field(pattern="^policy_[A-Za-z0-9_-]+$")]
