# Observability port

Contracts for the observability sink port. The current local POC uses bounded
OpenTelemetry attributes and local Grafana inspection. Add JSON Schema here
only when an observability payload crosses a service boundary or becomes a
release gate.
